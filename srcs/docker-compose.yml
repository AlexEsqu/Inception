# docker build -t my-nginx-app .
# docker run -p 8080:80 my-nginx-app

volumes:
  WordPressDB:
  WordPressFiles:

services:
  nginx:
    image:
    build:
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html



